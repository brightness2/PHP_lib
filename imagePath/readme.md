# 使用方法
```
  $spacePath = [
         'one'=>'one',
        'two'=>'twowoo',
         'images'=>'images',
     ];
     //$spacePath 可选参数
     $f = new ImagePath($spacePath);
    
    $file =  $f->show();
```